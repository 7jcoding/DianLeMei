extends layout
//header
block header
//content
block content
  div.container.bs
    div.row
      div.col-md-9
        h1= title
        small node express
        p.lead Welcome to #{author}
        blockquote
            p
                -if(user != null){
                    i #{user.user_name + ' ' + user.user_power}，
                -}
                |凌冬将至.
                |我是黑暗中的利剑，长城上的守卫，抵御寒冷的烈焰，破晓时分的光线，唤醒眠者的号角，守护王国的坚盾。
      div#sidebar.col-md-3(role="navigation")
        div.list-group
          a.list-group-item.active(href="#") link
          a.list-group-item(href="#") link
          a.list-group-item(href="#") link
          a.list-group-item(href="#") link
//footer
block footer
  script(type="text/javascript").
    function info(){
        console.log('footer block.');
    }
    info();
